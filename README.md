Use `npm install restore --force`` for restoring. It has issues in dependencies, but it should work fine.

Use `npm run build`` for building app.

Results will be in:
 - ./css/main.css
 - ./js/main.js